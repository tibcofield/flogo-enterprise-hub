{
  "name": "pongo2-prompt",
  "type": "flogo:activity",
  "version": "1.0.0",
  "title": "Pongo2 AI Prompt Template",
  "description": "Processes Pongo2/Django templates for AI prompts with dynamic variable mapping. Uses pongo2 library for Go-native template processing.",
  "author": "Flogo Extension",
  "homepage": "",
  "settings": [{
      "name": "template",
      "type": "string",
      "required": true,
      "display": {
        "name": "Pongo2 Template",
        "description": "Enter your Pongo2 template with variables like {{ variable_name }}. Individual input fields will be auto-generated for each variable.",
        "type": "texteditor",
        "rows": 15
      }
  }],
  "inputs": [
    {
      "name": "templateVariables",
      "type": "params",
      "display": {
        "name": "JSON Schema for Template Variables",
        "description": "Add a parameter for each {{ variable }} detected in your template above. Parameter names should match your template variables exactly. Optionally, use utility generate_flogo_params.sh to generate the JSON schema.",
        "mappable": true
      }
    }
  ],
  "outputs": [
    {
      "name": "renderedPrompt",
      "type": "string",
      "description": "The rendered prompt after template processing"
    }
  ]
}